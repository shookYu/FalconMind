# PoC_test 场景说明文档

> **文档用途**：说明每个场景的测试验证目的、思路与途径  
> **最后更新**：2025-02-25  
> **关联文档**：[01_TEST_OUTLINE.md](./01_TEST_OUTLINE.md)（测试大纲）、[03_TEST_METHODOLOGY.md](./03_TEST_METHODOLOGY.md)（测试方法论）

---

## 一、文档说明

本文档按**场景**组织，对每个 PoC 测试场景说明：

| 维度 | 说明 |
|------|------|
| **验证目的** | 本场景要验证什么能力、解决什么风险 |
| **验证思路** | 测试策略、关注点、验收标准 |
| **验证途径** | 如何执行（环境、命令、工具）、如何判定通过 |

执行前请先阅读 [PoC_test/README.md](../README.md) 与各场景目录下的 README / 完整测试指南。

---

## 二、类别1：单机基础搜索场景（5 个）

### 场景 1.1：单机网格搜索（LAWN_MOWER）- 矩形区域

- **目录**：`01_scenario_1_1_lawn_mower_rect`
- **优先级**：P0

| 维度 | 内容 |
|------|------|
| **验证目的** | 验证单机网格搜索的端到端能力：Builder 创建搜索任务、配置矩形区域与 LAWN_MOWER 模式并生成 SDK 代码；SDK 的路径规划、飞行控制与状态上报；Viewer 的搜索区域/路径/实时位置展示；NodeAgent 遥测与任务执行；Cluster Center 任务创建与状态同步。 |
| **验证思路** | 端到端测试：准备数据 → Builder 建流程、生成代码 → SDK 规划 LAWN_MOWER 路径并（可选）在 PX4-SITL 中执行 → 在 Viewer 中检查区域、路径与 UAV 位置。覆盖三种开发模式（模式1 直接 SDK、模式2 Builder 生成代码、模式3 FlowExecutor 动态执行）。 |
| **验证途径** | 启动 Cluster Center(8003)、Builder(9001)、Viewer(9000/8080)、PX4-SITL、NodeAgent；进入 `01_scenario_1_1_lawn_mower_rect`，执行 `pytest -v` 或按 [Doc/01_COMPLETE_TEST_GUIDE.md](../01_scenario_1_1_lawn_mower_rect/Doc/01_COMPLETE_TEST_GUIDE.md) 执行；通过 API 断言（流程创建、代码生成、航点在区域内、路径连续）、Viewer 界面与日志确认。 |

---

### 场景 1.2：单机螺旋搜索（SPIRAL）- 圆形区域

- **目录**：`02_scenario_1_2_spiral_circle`
- **优先级**：P0

| 维度 | 内容 |
|------|------|
| **验证目的** | 验证单机螺旋搜索及圆形区域处理：Builder 配置圆形区域与 SPIRAL 模式并生成 C++/Python 代码；SDK 的 SPIRAL 路径规划与圆形区域多边形转换；Viewer 圆形区域与螺旋路径可视化。 |
| **验证思路** | 与 1.1 类似，端到端 + 三种模式；重点验证圆形几何处理、SPIRAL 算法与 C++/Python 双接口（模式1/2/3）。 |
| **验证途径** | 服务与 PX4-SITL 就绪后，在 `02_scenario_1_2_spiral_circle` 下 `pytest -v`；可用 `./run_test.sh --all` 或 `--cpp`/`--python`/`--mode1`/`--mode2`/`--mode3`；通过航点是否在圆内、螺旋形态及 Viewer 显示判定。 |

---

### 场景 1.3：单机 Z 字形搜索（ZIGZAG）- 不规则多边形

- **目录**：`03_scenario_1_3_zigzag_polygon`
- **优先级**：P0

| 维度 | 内容 |
|------|------|
| **验证目的** | 验证 ZIGZAG 搜索模式及复杂多边形处理：SDK 的 ZIGZAG 路径生成、多边形裁剪与路径优化；Viewer 不规则多边形显示；Builder 多边形坐标配置与代码生成。 |
| **验证思路** | 使用不规则五边形等输入，验证多边形裁剪与 ZIGZAG 路径在边界内的正确性及 Builder/Viewer 对多边形的支持。 |
| **验证途径** | 在场景目录下执行 `pytest -v`；校验航点落在多边形内、路径符合 ZIGZAG 形态、Viewer 多边形与路径一致。 |

---

### 场景 1.4：单机扇形搜索（SECTOR）- 扇形区域

- **目录**：`04_scenario_1_4_sector_fan`
- **优先级**：P0

| 维度 | 内容 |
|------|------|
| **验证目的** | 验证 SECTOR 搜索模式与扇形区域：SDK 的 SECTOR 路径生成与扇形几何处理；Viewer 扇形区域可视化；Builder 扇形参数（半径、角度、起始方向）配置。 |
| **验证思路** | 给定扇形（如半径 80m、角度 120°、正北起始），验证路径仅落在扇形内且覆盖合理，以及参数配置与可视化一致。 |
| **验证途径** | 在场景目录下 `pytest -v`；检查航点在扇形内、路径形态符合 SECTOR、Viewer 扇形与路径正确。 |

---

### 场景 1.5：单机航点列表搜索（WAYPOINT_LIST）- 自定义路径

- **目录**：`05_scenario_1_5_waypoint_list`
- **优先级**：P0

| 维度 | 内容 |
|------|------|
| **验证目的** | 验证航点列表模式与自定义路径：SDK 对航点列表、高度/速度变化及悬停行为的处理；Viewer 航点标记与连线；Builder 航点列表编辑与代码生成。 |
| **验证思路** | 使用预定义航点序列（含高度、速度、悬停时间），验证执行顺序、高度/速度变化与悬停时长，以及 Viewer 展示一致。 |
| **验证途径** | 在场景目录下 `pytest -v`；比对实际执行航点顺序与预期列表、悬停时间与 Viewer 显示。 |

---

## 三、类别2：单机高级功能场景（4 个）

### 场景 2.1：单机搜索 + 目标检测 + 事件上报

- **目录**：`06_scenario_2_1_search_detect_report`
- **优先级**：P0

| 维度 | 内容 |
|------|------|
| **验证目的** | 验证“搜索 → 检测 → 上报”完整链路：SDK 中检测节点（如 DummyDetectionNode）与事件上报节点运行；Viewer 显示检测结果标记；Cluster Center 接收并处理事件；Builder 对检测与上报节点的配置与代码生成。 |
| **验证思路** | 在 LAWN_MOWER 搜索中注入或模拟检测结果，验证检测触发、事件上报、Cluster Center 收包及 Viewer 展示一致性。 |
| **验证途径** | 启动全部服务与仿真；在场景目录 `pytest -v`；通过 API 或日志确认事件已上报、Cluster Center 已记录、Viewer 有对应标记。 |

---

### 场景 2.2：单机搜索 + 目标跟踪

- **目录**：`07_scenario_2_2_search_tracking`
- **优先级**：P1

| 维度 | 内容 |
|------|------|
| **验证目的** | 验证目标跟踪：SDK 中跟踪节点（如 SimpleTrackerBackend）、跟踪 ID 分配与轨迹记录；Viewer 跟踪轨迹显示；Builder 跟踪节点配置。 |
| **验证思路** | 在搜索中模拟“发现目标”，验证进入跟踪模式、轨迹持续更新与 Viewer 轨迹线一致。 |
| **验证途径** | 场景目录下 `pytest -v`；检查跟踪状态切换、轨迹点序列与 Viewer 轨迹。 |

---

### 场景 2.3：单机搜索 + 低电量返航

- **目录**：`08_scenario_2_3_search_low_battery`
- **优先级**：P1

| 维度 | 内容 |
|------|------|
| **验证目的** | 验证异常与返航：电量监控、低电量阈值触发自动返航、返航路径规划；Viewer 返航状态展示；Cluster Center 接收低电量告警。 |
| **验证思路** | 模拟电量从 60% 降至 20%，验证在阈值触发返航、路径回到起飞点/指定点，以及告警与状态同步。 |
| **验证途径** | 在场景目录下 `pytest -v`（可配合 Mock 电量）；断言返航被触发、返航路径有效、Cluster Center 有告警、Viewer 状态正确。 |

---

### 场景 2.4：单机搜索 + 任务暂停/恢复

- **目录**：`09_scenario_2_4_search_pause_resume`
- **优先级**：P1

| 维度 | 内容 |
|------|------|
| **验证目的** | 验证任务控制：SDK 对暂停/恢复的处理、状态保存与恢复；Cluster Center 下发暂停/恢复命令；Viewer 任务状态显示。 |
| **验证思路** | 在 ZIGZAG 搜索过程中下发暂停，等待一段时间后恢复，验证暂停时无前进、恢复后从未完成航点继续。 |
| **验证途径** | 场景目录下 `pytest -v`；通过 API 下发控制命令，检查 SDK 状态与 Viewer 状态一致、恢复后进度连续。 |

---

## 四、类别3：多机基础协同场景（4 个）

### 场景 3.1：多机等分区域搜索（2 机）

- **目录**：`10_scenario_3_1_multi_equal_split`
- **优先级**：P0

| 维度 | 内容 |
|------|------|
| **验证目的** | 验证多机任务分配与等分区域：Cluster Center 等分区域分割（如纬度方向）、按电量等策略分配任务；多机独立执行 LAWN_MOWER；Viewer 多机位置与子区域显示；进度聚合。 |
| **验证思路** | 2 架 UAV、矩形区域，验证分割结果无重叠无遗漏、每机只飞自己子区域、Cluster Center 汇总进度正确。 |
| **验证途径** | 启动多 NodeAgent 或仿真多机；在场景目录 `pytest -v`；校验子区域并集等于总区域、各机航点仅在本区、进度聚合与预期一致。 |

---

### 场景 3.2：多机 Voronoi 区域搜索（3 机）

- **目录**：`11_scenario_3_2_multi_voronoi`
- **优先级**：P0

| 维度 | 内容 |
|------|------|
| **验证目的** | 验证 Voronoi 区域分割与基于位置的分配：Cluster Center 的 Voronoi 分割与冲突检测；多机在各自 Voronoi 单元内执行 SPIRAL；Viewer 显示 Voronoi 单元。 |
| **验证思路** | 3 架 UAV、给定初始位置，验证 Voronoi 单元无重叠覆盖目标区域、每机路径限于本单元。 |
| **验证途径** | 多机环境 + 场景目录 `pytest -v`；检查分割几何、路径与单元归属、Viewer 显示。 |

---

### 场景 3.3：多机农业喷洒（4 机）

- **目录**：`12_scenario_3_3_multi_agri_spraying`
- **优先级**：P0

| 维度 | 内容 |
|------|------|
| **验证目的** | 验证农业喷洒任务：Cluster Center 创建喷洒任务、配置喷洒参数（速率、高度、重叠率）；SDK 喷洒路径与重叠处理；Viewer 喷洒进度；Cluster Center 覆盖率统计。 |
| **验证思路** | 4 机、矩形农田，验证区域分割、喷洒参数生效、路径重叠满足要求、覆盖率统计合理。 |
| **验证途径** | 在场景目录 `pytest -v`；校验任务类型与参数、路径重叠、覆盖率数值与预期范围。 |

---

### 场景 3.4：多机协同搜索 + 目标发现协同

- **目录**：`13_scenario_3_4_multi_cooperative`
- **优先级**：P1

| 维度 | 内容 |
|------|------|
| **验证目的** | 验证多机协同机制：Cluster Center 处理“发现目标”等协同事件、任务重分配；SDK 动态路径重规划；Viewer 协同事件展示与协同目标跟踪。 |
| **验证思路** | 模拟 UAV1 发现目标后，UAV2/UAV3 前往确认，验证事件分发、重规划与 Viewer 协同状态。 |
| **验证途径** | 多机 + 场景目录 `pytest -v`；检查协同事件、重分配结果、重规划路径与 Viewer 显示。 |

---

## 五、类别4：多机高级协同场景（3 个）

### 场景 4.1：多机高级 Voronoi + 能力均衡

- **目录**：`14_scenario_4_1_advanced_voronoi`
- **优先级**：P1

| 维度 | 内容 |
|------|------|
| **验证目的** | 验证能力加权 Voronoi 与负载均衡：Cluster Center 的 AdvancedAreaSplitter、考虑电量等能力的分割与负载均衡；Viewer 能力分布展示。 |
| **验证思路** | 多机电量不同（如 90%、80%、70%、60%），验证分割后负载与能力匹配、无单机过载。 |
| **验证途径** | 场景目录 `pytest -v`；校验分割结果与电量/能力关系、Viewer 展示一致。 |

---

### 场景 4.2：多机冲突避免 + 路径重规划

- **目录**：`15_scenario_4_2_conflict_avoidance`
- **优先级**：P1

| 维度 | 内容 |
|------|------|
| **验证目的** | 验证冲突检测与路径重规划：Cluster Center 的位置/路径冲突检测、PathReplanner；SDK 按新路径执行；Viewer 冲突告警。 |
| **验证思路** | 构造 2 机路径交叉，验证检测到冲突、生成无冲突重规划、UAV 按新路径飞行。 |
| **验证途径** | 场景目录 `pytest -v`；检查冲突被识别、重规划结果无交叉、Viewer 告警与解消。 |

---

### 场景 4.3：多机 UAV 故障 + 任务重新分配

- **目录**：`16_scenario_4_3_failure_reassignment`
- **优先级**：P1

| 维度 | 内容 |
|------|------|
| **验证目的** | 验证故障检测与任务重分配：Cluster Center 故障检测、TaskReassigner、动态负载均衡；其余 UAV 接收并执行新分配子区域；Viewer 故障状态显示。 |
| **验证思路** | 模拟 UAV2 执行约 50% 后故障，验证未完成区域被重新分配给其他机、任务完成度与预期一致。 |
| **验证途径** | 场景目录 `pytest -v`；校验故障上报、重分配结果、新任务执行与 Viewer 状态。 |

---

## 六、类别5：边界和异常场景（2 个）

### 场景 5.1：极小区域搜索（边界测试）

- **目录**：`17_scenario_5_1_minimal_area`
- **优先级**：P2

| 维度 | 内容 |
|------|------|
| **验证目的** | 验证边界与参数校验：极小区域（如 10m×10m）、最低高度/速度/间距时，SDK 参数校验与边界处理、Builder 参数范围校验，不崩溃且行为合理。 |
| **验证思路** | 使用最小合法参数与临界非法参数，验证拒绝非法输入、合法边界下仍能生成可执行路径。 |
| **验证途径** | 场景目录 `pytest -v`；断言非法参数被拒绝、合法边界用例通过、无未处理异常。 |

---

### 场景 5.2：极大区域搜索（性能测试）

- **目录**：`18_scenario_5_2_large_area`
- **优先级**：P2

| 维度 | 内容 |
|------|------|
| **验证目的** | 验证大区域下的性能与稳定性：路径规划耗时、内存占用、Viewer 大范围渲染、Cluster Center 大数据量处理。 |
| **验证思路** | 使用大矩形（如 2000m×2000m），验证规划时间与内存在可接受范围内、Viewer 帧率与 Cluster Center 响应达标。 |
| **验证途径** | 场景目录 `pytest -v`；采集规划时间、内存、帧率等指标，与 01_TEST_OUTLINE 中的性能标准对比。 |

---

## 七、类别6：组合功能场景（2 个）

### 场景 6.1：完整端到端（单机）

- **目录**：`19_scenario_6_1_e2e_single`
- **优先级**：P0

| 维度 | 内容 |
|------|------|
| **验证目的** | 验证单机全链路：Builder 创建完整流程并生成代码 → SDK 执行搜索+检测+跟踪+上报+返航 → Viewer 全部可视化 → Cluster Center 任务管理 → NodeAgent 通信。 |
| **验证思路** | 不拆分子功能，一次性跑通“建流程 → 生成代码 → 执行 → 展示”，覆盖所有核心单机路径与组件交互。 |
| **验证途径** | 按完整测试指南启动所有服务与仿真；在场景目录执行长时间 E2E 测试；通过日志、Viewer 与 Cluster Center 状态综合判定通过。 |

---

### 场景 6.2：完整端到端（多机）

- **目录**：`20_scenario_6_2_e2e_multi`
- **优先级**：P0

| 维度 | 内容 |
|------|------|
| **验证目的** | 验证多机全链路：区域分割、任务分配、协同搜索、冲突避免、目标协同等全部多机能力在一次任务中配合完成；Cluster Center、SDK、Viewer 完整多机流程。 |
| **验证思路** | 多机从任务创建到完成全程自动化，验证所有多机功能路径与协同逻辑。 |
| **验证途径** | 多机环境 + 场景目录 E2E 测试；检查任务完成度、无死锁/长时间阻塞、Viewer 与 Cluster Center 数据一致。 |

---

## 八、通用验证途径摘要

- **环境**：见 [00_COMPLETE_TEST_EXECUTION_GUIDE.md](./00_COMPLETE_TEST_EXECUTION_GUIDE.md) 或各场景 `Doc/01_COMPLETE_TEST_GUIDE.md`；通常需 Cluster Center、Builder、Viewer、PX4-SITL、NodeAgent。
- **执行**：进入对应场景目录，执行 `pytest -v`；部分场景提供 `run_test.sh`。
- **三种模式**：模式1 直接 SDK、模式2 Builder 生成代码、模式3 FlowExecutor；见 [05_THREE_MODES_TESTING_GUIDE.md](./05_THREE_MODES_TESTING_GUIDE.md)。
- **通过标准**：功能按场景验证点全部满足；性能符合 [01_TEST_OUTLINE.md](./01_TEST_OUTLINE.md) 要求；无未处理异常或断言失败。

---

## 九、文档维护

- 新增场景时，在本文档中增加对应小节：验证目的、验证思路、验证途径。
- 若某场景有独立说明（如 README、01_COMPLETE_TEST_GUIDE），在“验证途径”中注明“详见 xxx”。
- 与 [01_TEST_OUTLINE.md](./01_TEST_OUTLINE.md)、[PHASE_5_SCENARIOS_DESIGN.md](../../PHASE_5_SCENARIOS_DESIGN.md) 保持一致，优先级与目录名以 TEST_OUTLINE 为准。
