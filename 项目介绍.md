# FalconMind 项目介绍

## 项目概述

**FalconMind** 是一套面向无人机的**AI感知与任务编排系统**，专为 Rockchip AI 芯片平台设计，提供从算法推理到任务执行的完整解决方案。

### 核心定位

- **目标用户**: 无人机行业工程师、算法工程师、系统集成商
- **核心价值**: 零代码/低代码快速构建无人机AI业务流程
- **技术特色**: 多平台统一开发体验（x86 + ARM64）

### 系统组成

FalconMind 由四个核心子系统组成：

```
┌─────────────────────────────────────────────────────────────────┐
│                     FalconMind 系统架构                          │
├─────────────────────────────────────────────────────────────────┤
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │   Builder    │  │   Viewer     │  │ClusterCenter │          │
│  │  可视化编排   │  │  实时监控    │  │  集群管理    │          │
│  │   (Vue3)     │  │  (Cesium)    │  │   (MQTT)     │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
│         │                 │                 │                   │
│         └─────────────────┴─────────────────┘                   │
│                         │                                       │
│              ┌──────────┴──────────┐                           │
│              │   FalconMindSDK     │                           │
│              │   C++17 SDK核心     │                           │
│              │  Pipeline + Node    │                           │
│              └──────────┬──────────┘                           │
│                         │                                       │
│              ┌──────────┴──────────┐                           │
│              │   Rockchip NPU      │                           │
│              │  RK3588/RK3576/...  │                           │
│              └─────────────────────┘                           │
└─────────────────────────────────────────────────────────────────┘
```

---

## 一、FalconMindSDK - 核心SDK

### 1.1 核心概念

SDK 采用 **Pipeline + Node** 架构，灵感来自 GStreamer 和 ROS：

#### Pipeline（流程）
- 数据流的容器和管理器
- 负责节点的生命周期管理
- 支持状态机（Ready → Running → Paused → Stopped）

#### Node（节点）
- 数据处理的基本单元
- 通过 Pad（端口）进行数据交换
- 支持 Source（生产数据）、Sink（消费数据）、Processor（处理数据）三种类型

#### Pad（端口）
- 节点的数据输入/输出接口
- 支持动态连接和断开
- 类型安全的数据传输

### 1.2 核心模块

```
SDK 模块结构:
├── Core（核心）
│   ├── Pipeline    - 流程编排引擎
│   ├── Node        - 节点基类
│   ├── Pad         - 数据端口
│   ├── Bus         - 消息总线
│   └── NodeFactory - 节点工厂
├── Perception（感知）
│   ├── Detection   - 目标检测
│   ├── Tracking    - 目标跟踪
│   └── SLAM        - 视觉定位
├── Sensors（传感器）
│   ├── Camera      - 相机采集
│   ├── IMU         - 惯性测量
│   └── GNSS        - 卫星定位
├── Flight（飞行控制）
│   ├── FlightConnection - 飞控连接
│   └── FlightCommand    - 飞行指令
└── Mission（任务）
    └── BehaviorTree - 行为树
```

### 1.3 代码示例

创建简单的数据处理流程：

```cpp
#include "falconmind/sdk/core/Pipeline.h"
#include "falconmind/sdk/core/Node.h"

using namespace falconmind::sdk;

int main() {
    // 1. 创建 Pipeline
    PipelineConfig cfg{"pipeline_001", "My Pipeline"};
    auto pipeline = std::make_shared<core::Pipeline>(cfg);
    
    // 2. 创建节点
    auto source = std::make_shared<CameraSourceNode>("camera");
    auto detect = std::make_shared<DetectionNode>("detector");
    auto sink = std::make_shared<DisplaySinkNode>("display");
    
    // 3. 添加节点到 Pipeline
    pipeline->addNode(source);
    pipeline->addNode(detect);
    pipeline->addNode(sink);
    
    // 4. 连接节点
    pipeline->link("camera", "frame", "detector", "frame");
    pipeline->link("detector", "detections", "display", "input");
    
    // 5. 启动流程
    pipeline->setState(core::PipelineState::Running);
    
    return 0;
}
```

### 1.4 平台支持

| 平台 | 架构 | NPU算力 | 适用场景 |
|------|------|---------|----------|
| **x86** | x86_64 | - (CPU/GPU) | 开发测试 |
| **RK3588** | ARM64 | 6TOPS × 3 | 高性能边缘计算 |
| **RK3576** | ARM64 | 6TOPS | 边缘AI部署 |
| **RV1126B** | ARM64 | 3TOPS | 入门级边缘设备 |

---

## 二、FalconMindBuilder - 可视化编排工具

### 2.1 产品定位

**Builder** 是面向行业工程师的**零代码/低代码**业务流程构建工具。

### 2.2 核心功能

1. **节点库管理**
   - 从 SDK 自动导入节点定义
   - 分类显示可用节点模板
   - 支持自定义节点扩展

2. **可视化编辑器**
   - 拖拽式节点布局
   - 可视化连接数据流
   - 实时显示流程拓扑

3. **代码生成**
   - 一键生成 SDK 工程骨架
   - 自动生成 main.cpp
   - 自动生成 CMakeLists.txt

### 2.3 技术栈

- **前端**: Vue3 + 原生 JS
- **后端**: FastAPI (Python)
- **通信**: HTTP REST API
- **地图**: Cesium (三维地球)

### 2.4 使用流程

```
1. 拖拽节点 → 2. 连接端口 → 3. 配置参数 → 4. 生成代码
```

生成的代码示例：

```cpp
// 由 Builder 自动生成的 main.cpp
#include <falconmind/sdk/core/Pipeline.h>
#include <falconmind/sdk/sensors/CameraSourceNode.h>
#include <falconmind/sdk/perception/DummyDetectionNode.h>

int main() {
    falconmind::sdk::core::Pipeline pipeline;
    
    auto camera = std::make_shared<sensors::CameraSourceNode>("camera");
    auto detector = std::make_shared<perception::DummyDetectionNode>("detector");
    
    pipeline.addNode(camera);
    pipeline.addNode(detector);
    pipeline.link(camera, "frame", detector, "frame");
    
    pipeline.setState(falconmind::sdk::core::PipelineState::RUNNING);
    
    return 0;
}
```

---

## 三、FalconMindViewer - 实时监控工具

### 3.1 产品定位

**Viewer** 提供无人机的实时状态监控和三维可视化展示。

### 3.2 核心功能

1. **三维可视化**
   - Cesium 地球仪展示 UAV 位置
   - 实时轨迹绘制
   - 多机同时展示

2. **遥测面板**
   - 位置信息 (Lat/Lon/Alt)
   - 姿态信息 (Roll/Pitch/Yaw)
   - 电量状态
   - GPS 状态
   - 链路质量
   - 飞行模式

3. **实时数据流**
   - WebSocket 实时推送
   - 低延迟数据更新 (< 100ms)

### 3.3 技术栈

- **前端**: CesiumJS + 原生 JavaScript
- **后端**: FastAPI + WebSocket
- **协议**: WebSocket + HTTP REST

### 3.4 数据流

```
UAV → MQTT → Viewer-Backend → WebSocket → Viewer-Frontend (Cesium)
```

---

## 四、ClusterCenter - 集群管理中心

### 4.1 产品定位

**ClusterCenter** 负责任务调度、集群管理和多机协同控制。

### 4.2 核心功能

1. **任务调度**
   - 任务分配和负载均衡
   - 优先级管理
   - 任务状态追踪

2. **集群管理**
   - 多 UAV 状态监控
   - 在线/离线检测
   - 故障自动切换

3. **多机协同**
   - 编队飞行控制
   - 协同搜索任务
   - 冲突避免

### 4.3 通信协议

- **主协议**: MQTT
- **备用协议**: gRPC
- **实时流**: WebSocket

---

## 五、示例程序

项目包含 **41个示例程序**，涵盖从基础 API 到完整集成的各种场景：

### 基础示例 (01-05)
- 01_pipeline_basic - Pipeline 基础流程编排
- 02_node_factory - NodeFactory 动态节点创建
- 03_pad_data_transfer - Pad 数据传输
- 04_bus_messaging - Bus 消息总线
- 05_flow_executor - Flow 执行器

### 感知算法 (06-10)
- 06_rknn_yolo_inference - YOLO 目标检测
- 07_rknn_backend - RKNN 后端集成
- 08_rk3588_multi_npu - 多 NPU 并行推理
- 09_batch_inference - 批量推理优化
- 10_parallel_inference - 并行推理

### 传感器示例 (11-14)
- 11_camera_capture - 相机采集
- 12_imu_sensor - IMU 传感器
- 13_gnss_positioning - GNSS 定位
- 14_lidar_pointcloud - LiDAR 点云

### 高级功能 (15-41)
- 15_detection_tracking - 目标跟踪
- 16_vins_fusion_slam - VINS 融合 SLAM
- 27_behavior_tree_basic - 行为树基础
- 40_full_integration - 完整集成示例

每个示例支持 **4个平台** (x86/RK3576/RV1126B/RK3588)。

---

## 六、技术特点

### 6.1 高性能

- **NPU 加速**: 充分利用 Rockchip NPU (最高 6TOPS × 3)
- **零拷贝**: Pipeline 内数据零拷贝传输
- **内存池**: 预分配内存减少碎片化

### 6.2 灵活性

- **插件化**: 支持动态加载感知算法插件
- **可配置**: JSON 配置文件管理节点参数
- **可扩展**: 通过 NodeFactory 轻松扩展新节点

### 6.3 多平台

- **统一 API**: 一套代码适配所有平台
- **条件编译**: 自动处理平台差异
- **交叉编译**: 完善的工具链支持

---

## 七、项目结构

```
FalconMind/                          # 项目根目录
├── FalconMindSDK/                   # 核心 SDK
│   ├── include/falconmind/sdk/      # 公共头文件
│   │   ├── core/                    # 核心模块 (Pipeline/Node/Pad/Bus)
│   │   ├── perception/              # 感知模块 (Detection/Tracking)
│   │   ├── sensors/                 # 传感器 (Camera/IMU/GNSS/LiDAR)
│   │   ├── flight/                  # 飞行控制
│   │   └── mission/                 # 任务管理 (BehaviorTree)
│   ├── src/                         # 源代码实现
│   ├── tests/                       # 单元测试
│   ├── examples/                    # 41个示例程序 (01-41)
│   ├── demo/                        # 演示程序
│   ├── python/                      # Python 绑定
│   └── NodeAgent/                   # 节点代理子项目
│
├── FalconMindBuilder/               # 可视化编排工具
│   ├── backend/                     # FastAPI 后端
│   ├── frontend/                    # Vue3 前端
│   └── Doc/                         # 设计文档
│
├── FalconMindViewer/                # 实时监控工具
│   ├── backend/                     # FastAPI + WebSocket
│   ├── frontend/                    # Cesium 前端
│   └── Doc/                         # 设计文档
│
├── ClusterCenter/                   # 集群管理中心
│   ├── backend/                     # 任务调度服务
│   ├── frontend/                    # 管理界面
│   └── Doc/                         # 设计文档
│
└── README.md                        # 项目说明
```

---

## 八、快速开始

### 8.1 环境准备

```bash
# Ubuntu 依赖
sudo apt-get update
sudo apt-get install -y build-essential cmake git python3 python3-pip
sudo apt-get install -y gcc-aarch64-linux-gnu g++-aarch64-linux-gnu
```

### 8.2 编译 SDK

```bash
cd FalconMindSDK/build
cmake .. -DCMAKE_BUILD_TYPE=Release
make -j4
make install  # 安装到 install/x86/
```

### 8.3 运行示例

```bash
cd FalconMindSDK/examples/01_pipeline_basic/x86/build
./01_pipeline_basic_x86
```

### 8.4 启动 Builder

```bash
# 后端
cd FalconMindBuilder/backend
python3 -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
uvicorn main:app --host 0.0.0.0 --port 9001

# 前端
cd FalconMindBuilder/frontend
python3 -m http.server 8001
```

访问 http://127.0.0.1:8001/index.html

### 8.5 启动 Viewer

```bash
# 后端
cd FalconMindViewer/backend
python3 -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
uvicorn main:app --host 0.0.0.0 --port 9000

# 前端
cd FalconMindViewer/frontend
python3 -m http.server 8000
```

访问 http://127.0.0.1:8000/index.html

---

## 九、应用场景

### 9.1 电力巡检
- 自动识别输电线路缺陷
- 实时传输巡检视频
- 自动生成巡检报告

### 9.2 搜索救援
- 大范围目标搜索
- 多机协同覆盖
- 热成像人员定位

### 9.3 农业植保
- 农田边界自动识别
- 精准喷洒控制
- 作业面积统计

### 9.4 安防监控
- 自动目标跟踪
- 异常行为检测
- 实时告警推送

---

## 十、总结

**FalconMind** 是一套完整的无人机 AI 解决方案：

- **SDK** 提供高性能的算法推理和任务编排能力
- **Builder** 让非专业程序员也能快速构建业务流程
- **Viewer** 提供直观的实时监控和三维可视化
- **ClusterCenter** 实现大规模集群管理和多机协同

通过四个子系统的紧密配合，FalconMind 能够帮助用户快速构建从算法到应用的完整无人机 AI 系统。

---

## 相关文档

- [SDK API 文档](FalconMindSDK/Doc/SDK_core_API.md)
- [Pipeline 设计文档](FalconMindSDK/Doc/Pipeline_Design.md)
- [Builder 设计文档](FalconMindBuilder/Doc/FalconMindBuilder_Design.md)
- [Viewer 设计文档](FalconMindViewer/Doc/FalconMindViewer_Design.md)
- [RKNN 集成指南](FalconMindSDK/Doc/RKNN_Integration.md)

---

*文档生成时间: 2026-02-13*
*项目版本: v1.0.0*
